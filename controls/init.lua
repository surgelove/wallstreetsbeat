-- ── CONTROLS LIBRARY ──
-- Load all controls and return them as a single table

return {
    theme      = require("controls.theme"),
    Button     = require("controls.button"),
    Slider     = require("controls.slider"),
    Background = require("controls.background"),
}
